// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://postgres1:1qWQy0tibVcRnFA8hl6N0LlxmXdLsHKp@dpg-ce1j3so2i3mkucf1cj30-a.singapore-postgres.render.com/apato_yx4s"
}

model user {
  id         Int             @id @default(autoincrement())
  name       String
  password   String
  email      String          @unique
  phone      String?         @unique
  address    String?
  role       ROLE            @default(USER)
  created_at DateTime        @default(now())
  updated_at DateTime        @updatedAt
  comments   apato_comment[]
  apato      apato[]
}

enum ROLE {
  ADMIN
  USER
  SELLER
}

model apato {
  id           Int             @id @default(autoincrement())
  creator      user            @relation(fields: [user_id], references: [id])
  title        String
  address      String
  image        String[]
  rating       Int?
  price        Int
  detail       String
  total_rating Float           @default(0)
  comments     apato_comment[]
  user_id      Int
}

model apato_comment {
  id      Int    @id @default(autoincrement())
  apato   apato? @relation(fields: [apatoId], references: [id])
  apatoId Int?
  user    user?  @relation(fields: [userId], references: [id])
  userId  Int?
  comment String
  rating  Int
}
